# secrets — never commit
.env
.env.*
!.env.example
*.key
keystore/

# deps
node_modules/
.pnpm-store/

# build
dist/
build/
out/
cache/
broadcast/
.next/
*.tsbuildinfo

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# local
.DS_Store
*.log
.recon/

# forge-std is cloned by setup, not vendored
contracts/lib/

# build docs: working notes, not part of the public repo
CLAUDE.md
ROADMAP.md
AGENTS.md
# OUR runbook, not the judge's front door. DEMO.md is a rehearsal log,
# CHECKLIST.md is a QA pass, telegram-questions.md was a message to a dev group,
# and DEMO-SCRIPT.md is the presenter's voiceover — it carries the lines to say
# on camera and the ones never to say. docs/ARCHITECTURE.md is the judge-facing
# companion to the README.
DEMO.md
docs/DEMO-SCRIPT.md
CHECKLIST.md
docs/telegram-questions.md
# Also internal by decision: the SDK feedback report is for the dreamDEX team
# directly, and the adversarial Q&A is our own stress-test of the submission.
# Both stay on disk and stay in retarget.ts so a redeploy rolls their addresses.
docs/sdk-feedback.md
docs/adversarial.md
*.dc.html
apps/web/dist/
.vercel

.vercel
.scratch/
.artifacts/

.tmp/
